{
  "name": "cognitive-campus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "install:all": "npm install --prefix server && npm install --prefix client"
  }
}
